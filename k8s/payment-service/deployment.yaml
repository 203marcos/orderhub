apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: orderhub
  labels:
    app: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/marcosdias/orderhub/payment-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: DB_URL
              value: jdbc:postgresql://postgres-payment:5432/payment_db
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-payment-secret
                  key: username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: postgres-payment-secret
                  key: password
            - name: KAFKA_SERVERS
              value: kafka:9092
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://jaeger:4317
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
